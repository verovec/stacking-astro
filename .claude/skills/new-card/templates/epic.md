---
id: ENN
title: <feature, ≤ 60 chars>
status: open       # open | done | dropped
cards: []          # child card IDs, integration order
---

# ENN · <title>

## Goal
<!-- 3–6 lines: the outcome when the epic is done, and what is explicitly OUT of
     scope. This is the only place the feature is described as a whole — child cards
     point here instead of repeating it. -->

## Card map
<!-- One line per child: `NNNN <title> — what it delivers (deps: …)`. Every child is
     independently mergeable; order = integration order. When later children depend on
     earlier findings, list only the ones specifiable today and end with a planning
     child that carves the rest. -->
